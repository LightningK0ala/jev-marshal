# Contribute

1. Fork the repository.
2. Create a branch.
3. Run `npm install`.
4. Make the change.
5. Run `npm run check`, `npm test`, and `npm run build`.
6. Open a pull request.

Do not commit API keys. Use `TYPESAFE_API_KEY` for live checks.
